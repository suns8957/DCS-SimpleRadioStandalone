<UserControl
    x:Class="Ciribob.DCS.SimpleRadio.Standalone.Client.UI.ClientWindow.ClientSettingsControl.GainDBControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800"
    Name="GainDBSlider">
    <StackPanel
        Margin="10">

        <Slider x:Name="GainDB"
                TickPlacement="BottomRight"
                SmallChange="5"
                LargeChange="10"
                IsSnapToTickEnabled="True"
                TickFrequency="10"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                Maximum="60"
                Value="{Binding ElementName=GainDBSlider, Path=GainDBValue, Mode=TwoWay}"
                Minimum="-100" />
        <Label FontSize="10" Content="{Binding ElementName=GainDBSlider, Path=GainDBValue}" ContentStringFormat="{}{0} dB" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>